class CurrentAccount extends Account {
    public CurrentAccount(String accountNumber, String name, String contact, String accountType, double balance) {
        super(accountNumber, name, contact, accountType, balance);
    }
}